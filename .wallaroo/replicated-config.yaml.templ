apiVersion: kots.io/v1beta1
kind: ConfigValues
spec:
    values:
        ingress_mode:
            value: "none"
        wallarooDomain:
            value: $CODESPACE_NAME-8443.$DOMAIN_SUFFIX           
        jupyter_mode:
            value: hub
        mothership_fqdn:
            value: api.$MS_BASE_URL
